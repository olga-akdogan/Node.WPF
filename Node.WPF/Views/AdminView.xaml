<UserControl x:Class="Node.WPF.Views.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel>
            <TextBlock Text="Admin" FontSize="28"/>
            <TextBlock Text="{Binding Status}" Opacity="0.7" Margin="0,6,0,0"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,12,0,12">
            <Button Content="Refresh" Command="{Binding RefreshCommand}" Width="120" Margin="0,0,10,0"/>
            <Button Content="Soft Delete" Command="{Binding SoftDeleteCommand}" Width="120" Margin="0,0,10,0"/>
            <Button Content="Restore" Command="{Binding RestoreCommand}" Width="120" Margin="0,0,10,0"/>
            <Button Content="Back" Command="{Binding BackCommand}" Width="120"/>
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <ListBox ItemsSource="{Binding Profiles}"
                     SelectedItem="{Binding SelectedProfile}"
                     DisplayMemberPath="BirthPlace"
                     />

            <Border Grid.Column="1" Margin="12,0,0,0" BorderBrush="#DDD" BorderThickness="1" CornerRadius="10" Padding="12">
                <StackPanel>
                    <TextBlock Text="Selected Profile" FontSize="16" Margin="0,0,0,10"/>

                    <TextBlock Text="{Binding SelectedProfile.AppUser.Email}" />
                    <TextBlock Text="{Binding SelectedProfile.AppUserId}" Opacity="0.7" Margin="0,2,0,10"/>

                    <TextBlock>
                        <Run Text="Deleted: "/>
                        <Run Text="{Binding SelectedProfile.IsDeleted}"/>
                    </TextBlock>

                    <TextBlock Margin="0,6,0,0">
                        <Run Text="DeleteAt: "/>
                        <Run Text="{Binding SelectedProfile.DeleteAt}"/>
                    </TextBlock>

                    <TextBlock Margin="0,10,0,0" TextWrapping="Wrap">
                        <Run Text="Birth: "/>
                        <Run Text="{Binding SelectedProfile.BirthDateTimeUtc}"/>
                    </TextBlock>

                    <TextBlock Margin="0,6,0,0" TextWrapping="Wrap">
                        <Run Text="Place: "/>
                        <Run Text="{Binding SelectedProfile.BirthPlace}"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>