<UserControl x:Class="Node.WPF.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="40" Width="320">
        <TextBlock Text="Register" FontSize="26" Margin="0,0,0,20"/>

        <TextBlock Text="Email"/>
        <TextBox x:Name="EmailBox" Margin="0,0,0,10"/>

        <TextBlock Text="Password"/>
        <PasswordBox x:Name="PwdBox" Margin="0,0,0,20"/>

        <Button Content="Create account" Height="40" Click="CreateAccount_Click"/>
        <TextBlock x:Name="ErrorText" Foreground="Tomato" Margin="0,10,0,0"/>
    </StackPanel>
</UserControl>
