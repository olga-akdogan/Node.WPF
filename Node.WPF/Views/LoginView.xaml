<UserControl x:Class="Node.WPF.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <StackPanel Margin="40" Width="320">
        <TextBlock Text="Login" FontSize="26" Margin="0,0,0,20"/>

        <TextBlock Text="Email"/>
        <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <TextBlock Text="Password"/>
        <TextBox Text="{Binding Password, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

        <Button Content="Login" Command="{Binding LoginCommand}" Height="34" Margin="0,10,0,0"/>
        <Button Content="Create account" Command="{Binding GoRegisterCommand}" Height="34" Margin="0,10,0,0"/>

        <TextBlock Text="{Binding Error}" Foreground="Tomato" Margin="0,10,0,0" TextWrapping="Wrap"/>
    </StackPanel>
</UserControl>